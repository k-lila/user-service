global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "microservices"
    metrics_path: "/actuator/prometheus"

    static_configs:
      - targets:
          - gateway:8081
          - user-service:8090
          - authorization-server:8082
